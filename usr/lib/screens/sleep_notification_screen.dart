import 'package:flutter/material.dart';

class SleepNotificationScreen extends StatelessWidget {
  const SleepNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sleep Notification'),
      ),
      body: const Center(
        child: Text('Sleep Notification functionality will be implemented here.'),
      ),
    );
  }
}
