import 'package:flutter/material.dart';

class RadiationAlertScreen extends StatelessWidget {
  const RadiationAlertScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Radiation Alerts'),
      ),
      body: const Center(
        child: Text('Radiation Alert functionality will be implemented here.'),
      ),
    );
  }
}
