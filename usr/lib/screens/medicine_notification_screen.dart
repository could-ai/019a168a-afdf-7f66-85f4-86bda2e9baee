import 'package:flutter/material.dart';

class MedicineNotificationScreen extends StatelessWidget {
  const MedicineNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Medicine Notification'),
      ),
      body: const Center(
        child: Text('Medicine Notification functionality will be implemented here.'),
      ),
    );
  }
}
